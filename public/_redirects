# Brewmie Cloudflare Pages routing.
#
# Root '/' serves the marketing landing page. All other paths fall through
# to direct file serving (assets, /privacy, /support, /delete-account,
# /ota/builds/*, /.well-known/*, etc.). The SPA's index.html still exists
# at dist/index.html for the Capacitor bundle; visiting brewmie.app/index.html
# directly would still hit it, but nothing on the marketing site links there.

/   /landing.html   200
